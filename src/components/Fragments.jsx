const Fragments = () => {
    return (<>

        <h1>heloooooo</h1>
        <p>helo para</p>
    </>);
}

export default Fragments;

{/* Fragments
 in react component , if we need to return the multile JSX 
we have to enclose it in one outer container.
to prevent the multiple duplicate divisions, we can use JSX  Fragments  that is --       <> </>   
its like a enclosing tag of singular form of all the elements or tags */ }

